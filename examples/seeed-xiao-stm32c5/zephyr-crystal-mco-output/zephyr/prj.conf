# GPIO is only used for the heartbeat LED.
CONFIG_GPIO=y

# PA9 is used as MCO2, so keep the UART console driver out of this sample.
CONFIG_SERIAL=n
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_PRINTK=n
